[project]
name = "atrx-screener"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "kiteconnect>=5.0.1",
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "requests>=2.31.0",
    "Flask>=3.0.0",
]

[tool.vercel]
entrypoint = "api.index:app"
